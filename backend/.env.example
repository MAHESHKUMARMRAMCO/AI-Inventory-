PORT=4001

# SQL Server connection
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=M08945_orderfulfillment
DB_USER=sa
DB_PASSWORD=changeme
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=true

# Shared secret the BFF must send as X-Internal-Token on every request.
# Also used by seed scripts calling this service directly.
INTERNAL_SHARED_SECRET=change-this-shared-secret
PRIORITY_PARTIAL_THRESHOLD_PERCENT=70
